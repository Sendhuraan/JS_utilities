Code cleanup in Jakefile.js to avoid duplicate in path names.
Set transpile as an option for jake browserTests:start[sourceDirName, es5 | es6].
Setup cleanup task to remove all the generated directories.